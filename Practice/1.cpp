#include <stdio.h>
#include <stdlib.h>
#include <math.h>


int main() 
{

    for(int i=0; i<11; i++)
    {
        printf("%d\n", 10-i);
        printf("น฿ป็\n");
    }
    

	return 0;

}


/*
    scanf("%d", &n);

    int *a = (int*)malloc(sizeof(int)*n);
    int *b = (int*)malloc(sizeof(int)*n);

    for(int i = 0; i < n; i++)
    {
        scanf("%d %d", &a[i], &b[i]);
    }

    for(int i = 0; i < n; i++)
    {
        printf("%d\n", a[i] + b[i]);
    }
*/


